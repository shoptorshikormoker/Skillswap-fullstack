<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.1</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.skillswap</groupId>
	<artifactId>backend</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>SkillSwap</name>
	<description>SkillSwap backend API</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>25</java.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.13.0</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.13.0</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>0.13.0</version>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>com.diffplug.spotless</groupId>
				<artifactId>spotless-maven-plugin</artifactId>
				<version>3.10.2</version>
				<configuration>
					<formats>
						<format>
							<includes>
								<include>pom.xml</include>
								<include>src/**/*.properties</include>
								<include>src/**/*.sql</include>
							</includes>
							<trimTrailingWhitespace/>
							<endWithNewline/>
						</format>
					</formats>
					<java>
						<palantirJavaFormat>
							<version>2.98.0</version>
						</palantirJavaFormat>
						<removeUnusedImports/>
						<formatAnnotations/>
						<trimTrailingWhitespace/>
						<endWithNewline/>
						<replaceRegex>
							<name>Put annotated method parameters on separate lines</name>
							<searchRegex>(?m)^(\h*)([^\r\n]*\()(@\w+(?:\([^\r\n]*?\))?)\h+(?=@)</searchRegex>
							<replacement>$1$2&#10;$1$1$3&#10;$1$1</replacement>
						</replaceRegex>
						<replaceRegex>
							<name>Put consecutive record annotations on separate lines</name>
							<searchRegex>(?m)^(\h*)(@\w+(?:\([^\r\n]*?\))?)\h+(?=@)</searchRegex>
							<replacement>$1$2&#10;$1</replacement>
						</replaceRegex>
						<replaceRegex>
							<name>Put parameter types below annotations</name>
							<searchRegex>(?m)^(\h*)(@\w+(?:\([^\r\n]*\))?)\h+((?:[\w.?&lt;>\[\]]+\h+)+\w+[,)])(\h*\{\}?)?\h*$</searchRegex>
							<replacement>$1$2&#10;$1$3$4</replacement>
						</replaceRegex>
					</java>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
